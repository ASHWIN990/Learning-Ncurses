#include <ncurses.h>
#include <stdio.h>
#include <stdlib.h>

// To compile "gcc -lncurses prog.c"

int main(int argc, char *argv[]){

    int ch, prev, row, col;
    prev = EOF;
    FILE *fp;
    int x, y;

    if(argc !)

}